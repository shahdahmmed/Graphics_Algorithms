int DrawRec(HDC hdc, int x1, int y1, int x2, int y2, COLORREF color) {
    DirectLineAlgorithm(hdc, x1, y1, x2, y1, color);
    DirectLineAlgorithm(hdc, x2, y1, x2, y2, color);
    DirectLineAlgorithm(hdc, x1, y2, x2, y2, color);
    DirectLineAlgorithm(hdc, x1, y1, x1, y2, color);
    return 0;
}
